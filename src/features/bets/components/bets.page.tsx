import BetsList from './bets-list';

const BetsPage = () => {
  return (
    <main
      style={{
        padding: '1rem',
      }}
    >
      <h1>Bets</h1>
      <BetsList />
    </main>
  );
};

export default BetsPage;
